# CRM 提交以最终快照建立幂等

T12 的首次 CRM create 使用 Lead 级稳定幂等键 `crm:create:{lead_id}`，并以一个 CRM Sync Record（CRM同步记录）保存该逻辑操作及其所有重试。规范化最终 Payload 的 snapshot hash 只用于审计和冻结提交事实，绝不参与 create 幂等键；因此 timeout/transport error 后必须复用同一记录、键和 payload，不能重新读表后再次 create。T13 的 update 幂等策略将在其自身实现中定义，且不得反向改变本首次创建契约。
