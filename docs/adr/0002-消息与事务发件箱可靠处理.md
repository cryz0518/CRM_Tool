# 消息与事务发件箱可靠处理

每条企业微信原始消息必须与对应待处理事件在同一数据库事务中持久化，再由异步工作进程执行多媒体处理、AI、企查查、智能表格和 CRM 调用；以 `message_id` 实施幂等。同一销售按持久化顺序串行处理，不同销售可以并行；失败消息在重试耗尽后成为顺序检查点，后续消息继续，日后人工重试仅以受保护的补充处理应用新字段补丁。每个外部适配器使用独立且可配置的重试策略，重试耗尽后进入“失败待处理”而不删除原始任务；CRM 自动重试最多 3 次。该选择优先保证外部依赖故障或连接重连时消息不丢失，而非追求同步处理的简洁性。
