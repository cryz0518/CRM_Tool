# AGENTS.md — CRM 线索自动录入项目实施说明

> 本文件用于指导 Codex 启动和持续开发本项目。
> 
> 项目目标：销售在企业微信机器人单聊中直接发送客户信息，AI（人工智能）自动识别、归并并增量写入企业微信智能表格；销售在同一张共享智能表格中仅查看/编辑自己的记录，最终审核后批量提交企业自研 CRM（客户关系管理系统）。

---

## 0. Codex 执行原则

1. **先跑通主链路，再扩能力。** 首期优先保证：消息接入稳定、客户不串线、智能表格可正确增量写入、销售人工修改不被覆盖、CRM（客户关系管理系统）可批量入库。
2. **AI（人工智能）只负责理解与建议，不直接决定业务结果。** 所有枚举校验、字段覆盖、去重、合并、权限、CRM（客户关系管理系统）提交必须由确定性业务代码控制。
3. **宁缺毋滥。** 不确定字段优先留空，不要为了“填满表格”而猜测。
4. **增量更新，不整行覆盖。** 每次仅更新本次新增或变化的字段。
5. **人工修改优先级最高。** 智能表格中销售人工修改后的字段，AI（人工智能）后续不得静默覆盖。
6. **去重只按公司名称。** 联系人、手机号、邮箱不参与去重判定。
7. **国内公司先标准化再去重。** 国内企业使用企查查进行标准公司名称匹配；国外企业允许直接填写公司名称。
8. **所有外部依赖做 Adapter（适配器）隔离。** 企业微信、智能表格、企查查、LLM（大语言模型）、CRM（客户关系管理系统）都必须通过独立 Client/Adapter（客户端/适配器）访问。
9. **所有阈值配置化。** 采集度、推送阈值、推送冷却时间、AI（人工智能）置信度阈值等不得写死在业务逻辑中。
10. **不要在代码中保存密钥。** CorpID（企业标识）、Secret（密钥）、模型 Key（密钥）、CRM Token（令牌）、企查查凭据必须通过环境变量或企业密钥管理系统注入。
11. **项目必须在 Docker（容器）环境下运行。** 开发、测试、PoC（概念验证）和部署均以 Docker Compose（容器编排）作为默认运行方式；不得以“本机直接运行 Python”作为唯一启动方式。必须保证新开发者执行 `docker compose up -d --build` 后即可启动核心依赖和应用。
12. **所有函数必须有中文说明。** 每个函数、方法、类方法都必须编写中文 Docstring（文档字符串），至少说明用途、关键参数、返回值、异常和重要副作用。
13. **所有业务代码必须被中文注释覆盖。** 每个非平凡的代码块、判断分支、循环、外部接口调用、数据转换、关键赋值和异常处理都必须添加中文注释；容易误解的单行逻辑必须使用中文行内注释或前置注释。纯语法结构（如空行、括号闭合、简单 import）可由所在代码块的中文注释统一说明，不要求机械地逐行重复无意义注释。
14. **日志是第一等工程能力。** 关键业务节点、外部接口调用、异常、重试、客户归属、去重、智能表格同步和 CRM（客户关系管理系统）提交必须记录结构化日志，并可通过 Docker 日志和前端管理界面查看。
15. **提供内部前端管理/调试界面。** 首期实现轻量 Operations Console（运维/调试控制台），用于查看系统健康状态、线索、消息归属、待归属消息、AI执行、智能表格同步、CRM同步、错误和业务审计日志；前端不是销售录入入口，销售仍以企业微信智能表格为审核界面。

---

# 1. 产品目标与业务边界

## 1.1 核心业务目标

销售在展会现场会同时接触大量客户，信息输入高度碎片化，可能包含：

- 文本描述；
- 客户名片图片；
- 聊天截图；
- 语音；
- 多轮补充信息；
- 对历史客户的再次补充；
- 一条消息中同时出现多个客户。

系统需要完成：

```text
销售发送消息
    ↓
企业微信机器人接收
    ↓
消息标准化
    ↓
客户归属判断
    ↓
AI（人工智能）字段提取
    ↓
国内公司企查查标准化
    ↓
公司名称去重
    ↓
Lead Draft（线索草稿）增量更新
    ↓
企业微信智能表格增量同步
    ↓
达到阶段性采集条件时推送同一张表格
    ↓
销售统一审核/修改
    ↓
AI（人工智能）生成备注
    ↓
最终校验 + CRM（客户关系管理系统）查重
    ↓
批量提交自研 CRM
```

## 1.2 首期非目标

首期不要实现：

- CRM（客户关系管理系统）内部商机跟进；
- 合同管理；
- 销售预测；
- BI（商业智能）报表体系；
- 复杂 Workflow Engine（工作流引擎）；
- 多渠道机器人；
- 为每名销售创建独立智能表格。

当前方案使用：**一张共享 CRM 线索智能表格 + Record-level Permission（记录级权限）隔离销售记录。**

---

# 2. 已确认的核心产品规则

## 2.1 自动开始录入

销售不需要输入“开始录入”。

只要销售进入企业微信机器人单聊并发送有效客户信息，系统立即进入自动录入流程。

非客户信息、闲聊、测试消息应识别为 `IGNORED（忽略）`，不得污染线索表。

## 2.2 一张共享智能表格

全企业使用一张 `CRM线索` 智能表格。

智能表格已经由管理员提前配置字段、字段类型及枚举选项。

普通销售通过智能表格原生内容权限，仅能查看/编辑：

```text
创建人包含查看者本人
OR
负责人包含查看者本人
```

机器人通过 API（应用程序接口）新增记录时，**必须把“负责人”写为当前发送消息的销售成员对象**，否则销售可能无法看到由机器人创建的记录。

管理员保留全表权限。

## 2.3 不按联系人去重

唯一业务去重规则：

```text
标准公司名称相同 => 认为是同一条线索
```

联系人、手机号、邮箱均不参与去重。

同一家公司出现不同联系人时：

- 不新增第二条线索；
- 原联系人字段默认不被 AI（人工智能）自动覆盖；
- 新联系人及联系方式进入补充信息，优先写入最终备注；
- 销售可在智能表格中手工将主联系人调整为最新联系人。

## 2.4 合并规则

检测到同公司记录时：

```text
旧字段为空 + 新字段有值
=> 自动补充

旧字段有值 + 新字段相同
=> 保持不变

旧字段有值 + 新字段不同
=> 不自动覆盖
=> 记录为补充/冲突信息
=> 销售最终审核
```

禁止 AI（人工智能）直接覆盖销售人工确认或已有非空字段。

## 2.5 智能表格不是唯一数据库

企业微信智能表格是 Review Workspace（审核工作台），不是后台唯一数据源。

后台必须保留：

- 原始企微消息；
- 消息与客户的归属关系；
- AI（人工智能）提取结果；
- 字段来源和置信度；
- 最后一次 AI（人工智能）写入值；
- 是否被销售人工修改；
- 企查查标准企业结果；
- CRM（客户关系管理系统）同步状态；
- 失败重试及审计信息。

---

# 3. CRM Field Registry（CRM 字段注册表）

智能表格业务字段必须与 CRM（客户关系管理系统）字段保持一致。

## 3.1 字段及类型

| 字段 | 智能表格建议类型 | AI（人工智能）策略 |
|---|---|---|
| 业务线 | 单选 | 只允许合法枚举，不能确定则留空 |
| 线索名称 | 文本 | 国内企查查标准化；国外直接填写 |
| 线索来源 | 单选 | 工博会场景默认“展会” |
| 联系人 | 文本 | 高置信度直接填，低置信度留空 |
| 职务 | 文本 | 高置信度填写，允许销售自由修改 |
| 沟通方式 | 单选 | 只允许合法枚举 |
| 手机 | 手机号/文本 | 格式校验后填写 |
| 电话 | 电话/文本 | 格式校验后填写 |
| 邮箱 | 邮箱 | 格式校验后填写 |
| 客户行业 | 单选 | 只允许合法枚举 |
| 客户级别 | 单选 | 只允许合法枚举 |
| 工艺 | 单选 | 无法匹配时可使用“其他” |
| 下次联系时间 | 日期 | 明确日期可自动解析，否则留空 |
| 附件 | 附件 | 名片/图片等自动关联 |
| 备注 | 长文本 | 其他字段审核后统一生成 |
| 地区定位 | 地理位置 | 保留智能表格位置类型 |
| 创建人 | 人员 | 系统写入 |
| 负责人 | 人员 | 必须写入当前销售成员 |

## 3.2 枚举定义

### 业务线

```text
协作机器人
车载机器人
```

### 线索来源

```text
促销
搜索引擎
广告
转介绍
线上注册
线上询价
预约上门
陌拜
电话咨询
邮件咨询
展会
信息渠道（通信运营商）
信息渠道（商会及协会）
信息渠道（政府职能部门）
信息渠道（异业合作）
```

### 沟通方式

```text
打电话
发邮件
发短信
见面拜访
活动
微信
陪访
线上会议
技术支持工单
技术支持跟进
```

### 客户行业

```text
3C电子
医疗
机械加工
汽车
食品
航空航天
精密制造
新能源
纺织
院校
其他
```

### 客户级别

```text
重点客户
普通客户
非优先客户
```

### 工艺

```text
装配
码垛
视觉检测
贴标
开箱机
自助加油/充电
商业应用
其他
```

## 3.3 “其他”处理

如果 AI（人工智能）识别到的实际值不在枚举中，但枚举存在“其他”：

```text
字段值 = 其他
实际识别内容 => 保存到 Enrichment（补充信息）
最终写入备注
```

示例：

```text
工艺 = 其他
备注补充：工艺：其他（焊接）
```

如果实际值本身也不确定：

```text
工艺 = 其他
备注补充：工艺：其他（请补充）
```

---

# 4. 备注生成规则

备注不要在每次消息到达时反复生成。

销售完成其他字段检查后，再统一调用 Remark Generator（备注生成器）。

固定输出结构：

```text
基本信息：XXX公司，位于XX城市，主要产品为XX，年销售额可达XX，所属行业为XXX行业。

线索需求：客户所需解决的具体问题或痛点

预算情况：客户大致预算范围

特殊要求：客户是否有特殊的要求信息备注
```

生成原则：

1. 不得编造销售未提供、企查查未返回、系统无法可靠推断的信息；
2. 某一段无有效信息时可以简化，不强行补齐句子；
3. CRM（客户关系管理系统）没有独立字段的信息，如预算、痛点、特殊要求、主要产品、年销售额等，作为备注素材；
4. 同公司新出现的联系人、电话等如果不能覆盖主字段，也进入备注素材；
5. “其他”枚举的实际说明追加在备注尾部；
6. 最终写入 CRM（客户关系管理系统）备注时按现有规则加前缀 `【AI录入】`。

---

# 5. 地区定位处理

智能表格中的 `地区定位` 保持地理位置类型。

CRM（客户关系管理系统）内部是“省 / 市 / 区 + 详细地址”结构时，由 Location Adapter（地区转换适配器）负责转换。

```text
智能表格 LOCATION（地理位置）
    ↓
地点标题 / 经纬度
    ↓
地址解析或 Reverse Geocoding（逆地理编码）
    ↓
省 / 市 / 区 / 详细地址
    ↓
CRM Adapter（CRM适配器）
```

优先级：

```text
销售人工选择地址
>
销售消息明确提到的地址
>
企查查注册地址
```

企查查注册地址只能作为候选/补充，不得静默覆盖销售明确提供的项目地点。

---

# 6. AI Lead Engine（AI 线索引擎）

## 6.1 AI（人工智能）职责

AI（人工智能）负责：

- 判断消息是否包含客户信息；
- 判断是否为新客户、当前客户补充、历史客户补充；
- 从文本、OCR（光学字符识别）、ASR（自动语音识别）文本中提取字段；
- 识别公司原始名称；
- 将自然语言映射到 CRM（客户关系管理系统）枚举；
- 提取需求、痛点、预算、特殊要求等补充信息；
- 给出置信度；
- 识别一条消息是否包含多个客户；
- 生成备注。

AI（人工智能）不得：

- 直接操作数据库；
- 直接写 CRM（客户关系管理系统）；
- 决定最终去重结果；
- 自动覆盖人工修改；
- 创造 CRM（客户关系管理系统）不存在的枚举值；
- 在低置信度下强行补值。

## 6.2 Structured Output（结构化输出）

所有 LLM（大语言模型）输出必须经过 Pydantic（数据校验模型库）约束。

建议结构：

```python
class LeadAnalysis:
    intent                # NEW_LEAD / UPDATE_LEAD / MULTI_LEAD / IGNORE
    customer_reference    # 公司、联系人、手机号等引用信息
    crm_fields            # 本轮建议更新的 CRM 字段，仅增量字段
    enrichment            # 预算、痛点、特殊要求、其他联系人等补充信息
    confidence_by_field   # 字段置信度
    conflicts             # 新旧信息冲突
    warnings              # 格式异常、敏感数据等
```

不要让模型输出整行 CRM（客户关系管理系统）记录后直接覆盖旧数据。

---

# 7. Lead Resolver（线索归属解析器）

这是首期最关键模块之一。

目标：判断每条消息应该新增一条线索还是更新已有线索。

归属优先级：

1. Explicit Signal（显式信号）：如“另外一个客户”“下一个”“刚又见了一个”；
2. Quote Relation（引用关系）：引用已归属消息时直接继承该线索；
3. Strong Identity（强身份信息）：标准公司名称优先，其次手机号/邮箱/联系人；
4. 当前客户上下文；
5. 近期历史消息；
6. AI（人工智能）语义判断；
7. 仍不能可靠判断时进入 `UNASSIGNED（待归属）`，不要默认塞给最后一位客户。

重要规则：

- 更新历史客户不一定切换当前客户；
- 一条消息中出现多个客户时拆分处理；
- 待归属消息允许后续消息回溯绑定；
- 所有归属行为应可审计。

---

# 8. Company Resolver（公司解析器）与去重

## 8.1 国内公司

流程：

```text
AI（人工智能）提取原始公司名
    ↓
企查查查询
    ↓
得到标准公司名称
    ↓
按标准公司名称去重
```

后台应保存企查查企业唯一标识用于审计和提高稳定性，但**正式去重业务规则仍以标准公司名称为准**。

如果企查查返回多个高度相似候选：

- 不要强行认定；
- 可以暂存候选；
- 线索名称无法可靠确定时允许留空或标记待完善；
- 后续由销售在智能表格审核时修正。

## 8.2 国外公司

国外公司允许自由填写。

去重前执行基础名称标准化，例如：

- 去除首尾空格；
- 统一连续空格；
- 大小写标准化用于比较；
- 不擅自翻译公司名称。

正式展示仍保留销售/AI（人工智能）最终确认文本。

## 8.3 表格内部去重

在写智能表格前：

```text
标准公司名称存在
    ↓
查询后台 Lead Draft（线索草稿）/智能表格现有记录
    ↓
已存在 => 更新对应 record_id（记录标识）
不存在 => 新增记录
```

提交 CRM（客户关系管理系统）前必须再做第二次公司名称查重。

---

# 9. Smart Table Sync（智能表格同步）

## 9.1 表格已经预先配置

项目代码**不要默认在启动时创建表结构**。

智能表格由管理员提前创建并配置：

- 字段；
- 枚举选项；
- 人员字段；
- 地理位置字段；
- 内容权限；
- 视图。

代码启动后应读取并缓存：

- `doc_id（文档标识）`；
- `sheet_id（子表标识）`；
- 各字段 `field_id（字段标识）`；
- 各单选字段的 `option_id（选项标识）`；
- 人员字段写入格式。

字段绑定优先按字段名称校验，如果缺字段或字段类型不匹配，启动健康检查必须报错。

## 9.2 增量写入

禁止整行覆盖。

新消息只产生 Field Patch（字段增量补丁）：

```text
本轮只新增：工艺、下次联系时间
=> 智能表格只更新这两个字段
```

## 9.3 User Edit Protection（人工编辑保护）

后台对每个 AI（人工智能）写过的字段保存 `last_ai_synced_value（AI最后同步值）`。

每次准备再次写入前：

```text
读取智能表格当前值

当前值 == last_ai_synced_value
=> 说明销售未修改，可继续AI更新

当前值 != last_ai_synced_value
=> 标记 user_modified（人工已修改）
=> AI禁止自动覆盖
```

销售人工编辑永远优先。

## 9.4 负责人

机器人新增任何记录时必须写：

```text
负责人 = 当前发送消息的销售 user_id（用户标识）
```

原因：智能表格使用记录级权限隔离销售数据。

---

# 10. Lead Readiness Score（线索采集度）与表格推送

采集度只决定“何时推送表格链接”，不决定是否创建记录。

低完整度客户同样需要保存到智能表格。

默认可配置参数：

```text
FIRST_PUSH_SCORE = 60
INCREMENTAL_PUSH_DELTA = 15
PUSH_COOLDOWN_SECONDS = 300
```

初版权重建议：

```text
线索名称                    25
联系人                      10
手机号/电话/邮箱至少一个     15
业务线                      10
明确需求/痛点                15
职务                         5
客户行业                     5
工艺                         5
沟通方式                     5
下次联系时间                 5
总计                       100
```

说明：

- 线索来源、创建人、负责人不参与采集度，因为由系统自动填；
- 附件不参与；
- 备注不参与，因为最终阶段才生成；
- 国内公司未完成可靠企查查标准化时，线索名称不应直接按满分计算；
- 参数必须可通过配置调整。

推送逻辑：

```text
某客户首次 score >= 60
=> 标记待推送

相较上次推送 score 增加 >= 15
=> 标记待推送

出现重要新增字段（标准公司、联系方式、业务线、明确需求、下次联系时间）
=> 可以标记待推送

距上次表格推送 < 5分钟
=> 不立即发送，合并提醒
```

机器人发送的始终是同一张共享 CRM 线索智能表格链接。

---

# 11. CRM Adapter（CRM 适配器）

CRM（客户关系管理系统）为企业自研系统。

禁止 AI（人工智能）服务直接操作 CRM 数据库。

必须通过 CRM Adapter（CRM适配器）统一封装：

```python
search_by_company_name()
create_lead()
update_lead()
merge_lead()
get_users()
validate_payload()
```

正式提交前：

1. 重新读取智能表格当前记录；
2. 形成 Final Snapshot（最终快照）；
3. 校验字段类型和枚举；
4. 解析地区定位；
5. 查重，只按公司名称；
6. 已存在则进入合并逻辑；
7. 未存在则新建；
8. 每条记录独立提交，禁止一条失败导致整个批次回滚；
9. CRM（客户关系管理系统）超时或 5xx 时异步重试；
10. 最多重试 3 次；
11. 必须做 Idempotency（幂等控制），防止重复创建。

---

# 12. 推荐技术栈

统一使用 Python（编程语言）。

```text
Python 3.12+
FastAPI（Python Web框架）
Pydantic v2（数据校验模型库）
SQLAlchemy 2（Python对象关系映射框架）
Alembic（数据库迁移工具）
PostgreSQL（关系型数据库）
Redis（内存缓存）
Celery（异步任务框架）
pytest（Python测试框架）
httpx（异步HTTP客户端）
wecom-aibot-python-sdk（企业微信AI机器人Python开发工具包）
Jinja2（服务端模板引擎）
HTMX（轻量前端交互库，可选）
Docker（容器运行环境）
Docker Compose（容器编排工具）
```

首期架构采用 Modular Monolith（模块化单体）：

```text
一个代码仓库
按模块划分
应用服务 + Worker（异步工作进程）分进程部署
暂不拆微服务
```


## 12.1 Docker（容器）运行要求

项目从第一天起按容器化方式开发，至少包含以下服务：

```text
app        FastAPI（Python Web框架）主应用 + 企业微信机器人连接
worker     Celery（异步任务框架）工作进程
postgres   PostgreSQL（关系型数据库）
redis      Redis（内存缓存 / Celery Broker）
```

可根据实际需要追加：

```text
scheduler  Celery Beat（定时任务调度器）
proxy      Nginx（反向代理，可在部署阶段增加）
```

强制要求：

- 根目录必须提供 `Dockerfile`、`docker-compose.yml`、`.dockerignore`；
- `docker compose up -d --build` 必须可以完成本地环境启动；
- `docker compose down` 可以安全停止环境；
- PostgreSQL（关系型数据库）数据必须使用 Volume（数据卷）持久化；
- 应用配置通过 `.env` / 环境变量注入，镜像内不得包含真实密钥；
- 所有容器需要配置 Healthcheck（健康检查）或由 `/health` 接口提供健康状态；
- 应用日志优先输出到 stdout/stderr（标准输出/标准错误），便于执行 `docker compose logs -f app` 和 `docker compose logs -f worker` 查看；
- 不允许把本地绝对路径、开发者机器路径写死进代码；
- Docker（容器）中的时区必须显式配置为项目部署环境要求，默认建议 `Asia/Shanghai`；
- 数据库 Migration（迁移）必须提供容器内可执行命令。

建议提供 `Makefile` 或脚本统一常用命令，例如：

```text
make up       # 构建并启动所有容器
make down     # 停止容器
make logs     # 查看应用日志
make test     # 在容器中执行测试
make migrate  # 执行数据库迁移
```

---

# 13. 推荐代码目录

```text
crm-ai-lead/
├── AGENTS.md
├── README.md
├── pyproject.toml
├── .env.example
├── .dockerignore
├── Dockerfile
├── docker-compose.yml
├── Makefile
│
├── app/
│   ├── main.py
│   │
│   ├── core/
│   │   ├── config.py
│   │   ├── logging.py
│   │   ├── exceptions.py
│   │   └── enums.py
│   │
│   ├── web/
│   │   ├── routes.py
│   │   ├── schemas.py
│   │   ├── services.py
│   │   ├── templates/
│   │   └── static/
│   │
│   ├── wecom/
│   │   ├── client.py
│   │   ├── handlers.py
│   │   ├── normalizer.py
│   │   └── schemas.py
│   │
│   ├── leads/
│   │   ├── models.py
│   │   ├── schemas.py
│   │   ├── resolver.py
│   │   ├── service.py
│   │   ├── merger.py
│   │   ├── readiness.py
│   │   └── repository.py
│   │
│   ├── ai/
│   │   ├── client.py
│   │   ├── extractor.py
│   │   ├── schemas.py
│   │   ├── prompts/
│   │   └── remark_generator.py
│   │
│   ├── smartsheet/
│   │   ├── client.py
│   │   ├── schema_loader.py
│   │   ├── sync_service.py
│   │   └── permissions.py
│   │
│   ├── qcc/
│   │   ├── client.py
│   │   └── company_resolver.py
│   │
│   ├── crm/
│   │   ├── client.py
│   │   ├── adapter.py
│   │   ├── validator.py
│   │   └── sync_service.py
│   │
│   ├── location/
│   │   └── adapter.py
│   │
│   ├── observability/
│   │   ├── audit.py
│   │   ├── events.py
│   │   └── repository.py
│   │
│   └── db/
│       ├── base.py
│       ├── session.py
│       └── migrations/
│
├── workers/
│   ├── celery_app.py
│   ├── ai_tasks.py
│   ├── media_tasks.py
│   ├── smartsheet_tasks.py
│   └── crm_tasks.py
│
└── tests/
    ├── unit/
    ├── integration/
    └── fixtures/
```

---

# 14. 数据模型（概念级）

暂时不要过度拆表，但至少需要以下概念：

## Sales Session（销售会话状态）

保存：

- 企业微信销售 `user_id（用户标识）`；
- 当前最可能正在交流的 `lead_id（线索标识）`；
- 最近一次智能表格推送时间；
- 会话更新时间。

## Lead Draft（线索草稿）

保存：

- CRM（客户关系管理系统）业务字段；
- 标准公司名称；
- 企查查企业标识；
- 智能表格 `record_id（记录标识）`；
- 负责人；
- 采集度；
- CRM（客户关系管理系统）同步状态。

## Lead Message（线索消息）

保存：

- 原始消息标识；
- 文本/OCR（光学字符识别）/ASR（自动语音识别）结果；
- 引用消息；
- 附件；
- 归属状态；
- 关联线索。

## Field Metadata（字段元数据）

保存：

- AI（人工智能）置信度；
- AI（人工智能）最后同步值；
- 是否人工修改；
- 来源消息；
- 候选值；
- 冲突信息。

## Enrichment（补充信息）

保存 CRM（客户关系管理系统）没有独立字段但备注需要的信息：

- 需求/痛点；
- 预算；
- 特殊要求；
- 主要产品；
- 年销售额；
- 其他工艺；
- 其他行业说明；
- 其他联系人/联系方式；
- 其他补充数据。

## CRM Sync Record（CRM 同步记录）

保存：

- CREATE（创建）/UPDATE（更新）/MERGE（合并）；
- 最终提交快照；
- CRM（客户关系管理系统）响应；
- 重试次数；
- CRM 线索 ID；
- 错误信息。

---

# 15. 消息处理主流程

每条企业微信消息按以下顺序执行：

```text
1. 接收消息
2. 消息幂等检查
3. 保存原始消息
4. 下载/转译图片、语音等媒体
5. AI（人工智能）判断是否客户信息
6. AI（人工智能）提取客户引用与字段补丁
7. Lead Resolver（线索归属解析器）定位客户
8. 如出现新公司，执行 Company Resolver（公司解析器）
9. 国内企业企查查标准化
10. 按标准公司名称执行去重
11. 创建或获取 Lead Draft（线索草稿）
12. 应用 Field Patch（字段增量补丁）
13. 保护人工修改字段
14. 更新 Enrichment（补充信息）
15. 计算 Lead Readiness Score（线索采集度）
16. 增量同步企业微信智能表格
17. 写入负责人为当前销售
18. 判断是否达到表格推送条件
19. 异步记录审计/指标
```

任何一步失败，都不得导致原始消息丢失。

---

# 16. 多模态输入

## 文本

直接进入 AI Lead Engine（AI线索引擎）。

## 图片

首期至少支持：

- 名片 OCR（光学字符识别）；
- 聊天截图 OCR（光学字符识别）。

处理：

```text
图片下载
→ OCR
→ normalized_text（标准化文本）
→ AI字段提取
```

## 语音

```text
语音下载
→ ASR（自动语音识别）
→ normalized_text（标准化文本）
→ AI字段提取
```

如果 OCR（光学字符识别）/ASR（自动语音识别）失败：

- 记录失败原因；
- 不阻塞后续消息；
- 机器人可发送简短失败提示。

---

# 17. 安全与隐私

至少满足：

- 销售只查看自己的记录；
- 管理员查看全部记录；
- 原始消息及客户数据只在企业内部授权系统流转；
- 手机号、邮箱等敏感数据日志默认脱敏；
- 外部 AI（人工智能）调用使用 HTTPS（安全超文本传输协议）；
- 禁止使用客户数据进行模型训练，除非企业另有明确授权；
- 身份证、银行卡等与 CRM（客户关系管理系统）线索无关的高度敏感信息不写入智能表格；
- 审计日志建议保留至少 180 天；
- API（应用程序接口）调用日志不得记录 Secret（密钥）/Token（令牌）明文。

---

# 18. 日志、监控与前端运维界面

## 18.1 Logging（日志）规范

日志必须从项目第一天开始实现，不允许等功能完成后再补。

所有日志至少包含：

```text
时间戳
environment（运行环境）
service（服务名）
level（日志级别）
message（中文日志说明）
request_id（请求标识）
message_id（企微消息标识，可为空）
lead_id（线索标识，可为空）
record_id（智能表格记录标识，可为空）
wecom_user_id（企微用户标识，可为空）
```

关键要求：

- 日志内容尽量使用中文说明，技术字段名保持稳定；
- 使用 Structured Logging（结构化日志），生产环境推荐 JSON（结构化数据）格式输出；
- 开发环境允许使用更易读的 Console Format（控制台格式）；
- 日志级别至少支持 `DEBUG / INFO / WARNING / ERROR / CRITICAL`；
- `LOG_LEVEL` 必须环境变量可配置；
- 任何 Secret（密钥）、Token（令牌）、Cookie（会话凭据）不得写入日志；
- 手机号、邮箱等敏感字段输出日志时必须脱敏；
- 外部接口日志记录接口名称、耗时、返回状态和错误摘要，不记录完整敏感 Payload（负载）；
- 异常日志必须包含 Traceback（异常堆栈），便于定位；
- 每次重试必须记录当前重试次数和最大重试次数；
- 所有业务合并、去重、客户归属、人工修改保护行为需要有可审计事件。

Docker（容器）环境下的最低查看方式：

```text
docker compose logs -f app
docker compose logs -f worker
```

不要依赖单一日志文件作为唯一查看方式。

## 18.2 Business Audit Log（业务审计日志）

除了运行时日志，还需要保存可查询的业务审计事件。至少包含：

```text
客户创建
客户归属调整
同公司去重命中
字段自动补充
字段冲突
检测到销售人工修改
智能表格新增/更新失败
企查查匹配结果
AI执行成功/失败
CRM创建/合并/失败/重试
```

业务审计日志应进入数据库，便于前端查询；不要通过读取 Docker 容器日志来实现业务审计页面。

## 18.3 Metrics（指标）

至少记录以下 Metrics（指标）：

```text
收到企微消息数
有效客户消息数
新建线索数
已有线索增量更新数
待归属消息数
公司查重命中数
AI字段填写数
人工修改字段数
智能表格同步成功率
CRM提交成功率
CRM重复命中率
AI调用耗时
智能表格API耗时
CRM API耗时
```

## 18.4 Operations Console（运维/调试控制台）

必须提供一个内部前端界面，目的不是替代企业微信智能表格，而是帮助开发、测试、产品和管理员快速检查系统运行情况。

首期优先使用 Python（编程语言）主栈实现，建议采用：

```text
FastAPI（Python Web框架）
+ Jinja2（服务端模板引擎）
+ HTMX（轻量前端交互库，可选）
```

不要为了运维界面单独引入复杂前端工程，除非后续产品需求明确扩大。

MVP（最小可行产品）至少提供以下页面：

1. **系统概览**：应用、数据库、Redis（内存缓存）、Worker（工作进程）、企业微信连接状态；
2. **线索列表**：查看 Lead Draft（线索草稿）、标准公司名、负责人、采集度、智能表格 record_id（记录标识）、CRM同步状态；
3. **消息列表**：查看原始消息、标准化文本、归属客户及处理状态；
4. **待归属消息**：集中查看 `UNASSIGNED（待归属）` 消息，便于调试归属算法；
5. **AI执行记录**：模型、耗时、结果状态、解析错误，不展示敏感密钥；
6. **智能表格同步**：新增/更新记录状态、失败原因、最近同步时间；
7. **CRM同步**：创建/合并/重试状态及错误原因；
8. **业务日志/错误日志**：按时间、级别、lead_id（线索标识）、message_id（消息标识）筛选业务审计和异常记录；
9. **配置状态**：只显示某项配置是否已设置，禁止展示 Secret（密钥）真实值。

前端界面要求：

- 只用于企业内部管理和调试；
- 默认不得暴露到公网；
- 必须预留 Authentication（身份认证）与 Authorization（权限控制）接口；
- 真实企业身份认证方式尚未确定前，先通过 Interface（接口）抽象，不要写死不安全的长期方案；
- 禁止通过前端直接展示完整 Token（令牌）、Secret（密钥）或未经脱敏的敏感数据；
- 页面优先可用、清晰、便于排查问题，不追求复杂视觉效果。

---

# 19. 测试策略

## 19.1 Unit Test（单元测试）

测试代码同样遵守中文注释要求：每个测试函数必须用中文 Docstring（文档字符串）说明验证目标，复杂 Fixture（测试夹具）和 Mock（模拟实现）必须有中文注释。

必须覆盖：

- CRM（客户关系管理系统）枚举校验；
- 公司名称标准化；
- 去重逻辑；
- 合并逻辑；
- 人工修改保护；
- 采集度计算；
- 表格推送条件；
- 地址转换；
- CRM（客户关系管理系统）提交幂等。

## 19.2 Integration Test（集成测试）

必须覆盖：

- 企业微信消息 -> Lead Draft（线索草稿）；
- Lead Draft（线索草稿） -> 智能表格新增记录；
- 同公司第二条信息 -> 更新原记录而不是新增；
- API（应用程序接口）创建记录且负责人=销售 -> 销售可以查看记录；
- 销售人工修改字段 -> AI后续不覆盖；
- 智能表格最终值 -> CRM Payload（CRM提交数据）；
- CRM（客户关系管理系统）已有同公司 -> 合并而不是重复创建。

## 19.3 必须准备的场景测试

1. 单条完整文本；
2. 同一客户连续 5 条碎片消息；
3. 新客户紧接上一个客户；
4. 一条消息两个客户；
5. 引用旧名片后补充信息；
6. 同一公司第二联系人；
7. 同一公司不同工艺；
8. 国内公司简称 -> 企查查标准企业；
9. 国外公司自由填写；
10. AI（人工智能）低置信度字段留空；
11. 销售人工修改枚举字段；
12. 同公司重复提交 CRM（客户关系管理系统）；
13. CRM（客户关系管理系统）超时；
14. OCR（光学字符识别）失败；
15. ASR（自动语音识别）失败。

---

# 20. 开发阶段与实施顺序

## Phase 0（阶段0）：PoC 概念验证

优先验证，不做完整业务。**Phase 0 从第一天就必须在 Docker（容器）环境运行。**

1. 创建 `Dockerfile`、`docker-compose.yml`、`.dockerignore`，通过 Docker Compose（容器编排）启动 app / worker / postgres / redis；
2. 使用 `wecom-aibot-python-sdk（企业微信AI机器人Python开发工具包）` 接收测试销售单聊消息；
3. 读取当前 `CRM线索` 智能表格结构；
4. 用 API（应用程序接口）新增一条测试记录；
5. 把“负责人”写成测试销售的真实企业微信 `user_id（用户标识）`；
6. 验证销售本人可见、其他销售不可见；
7. 读取新增记录，取得 `record_id（记录标识）`；
8. 使用 `record_id（记录标识）` 增量更新同一条记录；
9. 验证单选、人员、日期、附件、位置字段的 API（应用程序接口）读写格式。

**Phase 0（阶段0）未通过前，不进入完整AI开发。**

## Phase 1（阶段1）：文本 MVP（最小可行产品）

实现：

- 文本消息；
- AI（人工智能）结构化提取；
- 当前客户上下文；
- 新客户判断；
- 国内企查查标准化；
- 公司名称去重；
- Lead Draft（线索草稿）；
- 智能表格新增/增量更新；
- 负责人权限；
- 采集度及推送；
- 人工修改保护；
- 基础 Operations Console（运维/调试控制台），至少能查看系统健康、线索、消息、待归属消息和错误日志。

## Phase 2（阶段2）：CRM 闭环

实现：

- 最终读取智能表格；
- 备注生成；
- CRM（客户关系管理系统）字段校验；
- 公司名称查重；
- 创建/合并；
- 幂等；
- 异步重试；
- 提交结果反馈。

## Phase 3（阶段3）：多模态与复杂归属

实现：

- 名片 OCR（光学字符识别）；
- 截图 OCR（光学字符识别）；
- 语音 ASR（自动语音识别）；
- 引用消息；
- 多客户单消息拆分；
- 待归属消息回溯；
- 历史客户复杂补充。

## Phase 4（阶段4）：试点优化

使用真实销售试点数据优化：

- AI（人工智能）字段准确率；
- 客户串线率；
- 公司企查查匹配准确率；
- 采集度阈值；
- 推送频率；
- 备注质量；
- 销售人工修改率；
- CRM（客户关系管理系统）重复率。

---

# 21. Codex 启动时的第一批任务

Codex 第一次进入仓库后，按顺序执行：

1. 创建 Python（编程语言）项目骨架和 `pyproject.toml`；
2. 创建 `.env.example`，只放变量名，不放真实密钥；
3. 创建 `Dockerfile`、`.dockerignore`、`docker-compose.yml` 和必要的数据 Volume（数据卷）；
4. 确保 `docker compose up -d --build` 可以启动 app / worker / postgres / redis；
5. 创建模块目录；
6. 配置 Ruff（Python静态检查工具）、mypy（Python类型检查工具）、pytest（Python测试框架）；
7. 建立统一中文代码注释规范、中文 Docstring（文档字符串）模板和代码审查约束；
8. 建立配置模型；
9. 建立 Structured Logging（结构化日志）模块，支持 `request_id / message_id / lead_id / record_id / wecom_user_id`；
10. 建立企业微信 SDK（开发工具包）最小连接示例；
11. 建立 SmartTableClient（智能表格客户端）接口和 Mock（模拟实现）；
12. 建立 `GET /health` 健康检查；
13. 建立智能表格 Schema Check（结构检查）命令；
14. 建立最小 Operations Console（运维/调试控制台），先实现健康状态和日志/审计列表页面；
15. 写 Phase 0（阶段0）PoC 脚本；
16. PoC 成功后再创建数据库 Migration（迁移）和业务模型；
17. 再实现 AI Lead Engine（AI线索引擎）。

**上述任务必须在 Docker（容器）中可执行。不要一开始就同时实现所有业务能力。**

---

# 22. Definition of Done（完成定义）

MVP（最小可行产品）完成至少满足：

- 销售无需“开始录入”，直接发消息即可；
- 文本客户信息可以自动进入共享智能表格；
- 记录负责人正确，销售之间不可互看；
- 连续碎片消息能够归并到同一客户；
- 切换新客户不会污染上一客户；
- 同公司名称不会新增重复行；
- 同公司新信息可以补充原记录；
- 原字段非空且新值冲突时不会自动覆盖；
- 销售人工修改后 AI（人工智能）不再覆盖；
- 国内公司可完成企查查标准化；
- CRM（客户关系管理系统）枚举只能写合法值；
- 低置信度字段可以保持为空；
- 达到采集度阈值后机器人可推送同一张智能表格；
- 销售审核后可以生成固定结构备注；
- 正式提交 CRM（客户关系管理系统）前再次按公司名称查重；
- CRM（客户关系管理系统）提交支持失败重试和幂等；
- 关键业务路径有自动化测试；
- 所有业务函数、方法和测试函数均有中文 Docstring（文档字符串）；
- 每个非平凡业务代码块、判断分支、循环、外部调用、数据转换、关键赋值和异常处理均有中文注释；
- 项目可以通过 `docker compose up -d --build` 在干净环境中完成构建和启动；
- app / worker / postgres / redis 均运行在 Docker（容器）中并具备可检查的健康状态；
- 日志能够通过 `message_id（消息标识）`、`lead_id（线索标识）`、`record_id（记录标识）` 追踪全链路；
- 开发和测试人员可以通过 `docker compose logs -f app` / `docker compose logs -f worker` 查看运行日志；
- Operations Console（运维/调试控制台）能够查看系统健康、线索、消息、待归属消息、AI执行、智能表格同步、CRM同步、错误与业务审计日志。

---

# 23. 当前仍需在开发对接时补齐的信息

以下信息不要在代码中猜测，使用 TODO（待办事项）明确等待真实接口资料：

- 企业微信智能表格真实 `doc_id（文档标识）`、`sheet_id（子表标识）`；
- 智能表格各字段真实 `field_id（字段标识）` 与选项 `option_id（选项标识）`；
- CRM（客户关系管理系统）真实 API（应用程序接口）地址、认证方式及字段 Key（键）；
- CRM（客户关系管理系统）人员 ID 与企业微信 `user_id（用户标识）` 映射方式；
- 企查查接口版本、返回字段和企业唯一标识；
- 具体 LLM Provider（大语言模型供应商）和模型名称；
- OCR（光学字符识别）/ASR（自动语音识别）具体供应商；
- 地区定位转换所用地图/地址服务；
- 最终触发“生成备注/提交CRM”的机器人交互方式。

这些信息未确认前，使用 Interface（接口）+ Mock（模拟实现）开发，不要把假定协议写死。

---

# 24. 需求依据

实现时以以下资料和本文件中的最新确认规则为准：

1. `工博会AI录入聊天信息成CRM线索 — 产品文档.pdf`
2. `工博会场景现场获客&录入——需求调研.pdf`
3. `数据和交互场景 · 长广溪智造.pdf`
4. 本次项目讨论中已经确认的最新架构和规则

如原始产品文档与本 `AGENTS.md` 在交互设计上有冲突，**优先使用本 `AGENTS.md` 的最新确认方案**，包括：

- 不再要求销售点击“开始录入”；
- 不再采用一人一张智能表格；
- 使用一张共享智能表格 + 负责人/创建人记录级权限；
- 不进行频繁逐字段确认；
- 智能表格承担统一审核工作台；
- 去重只根据标准公司名称；
- 同公司新字段合并，不自动覆盖已有非空字段；
- 最终备注在其他字段审核后生成；
- 项目默认在 Docker（容器）下开发、测试和运行；
- 所有函数与业务逻辑代码必须使用中文 Docstring（文档字符串）和中文注释；
- 必须提供结构化日志和内部 Operations Console（运维/调试控制台）。

---

# 25. 最终架构原则

本项目最终应保持以下分层：

```text
企业微信机器人
    ↓
Message Processor（消息处理器）
    ↓
Lead Resolver（线索归属解析器）
    ↓
AI Lead Engine（AI线索引擎）
    ↓
Company Resolver（公司解析器）
    ↓
Lead Domain（线索业务域）
    ├────────→ Operations Console（运维/调试控制台）
    ↓
Smart Table Sync（智能表格同步）
    ↓
销售人工审核
    ↓
Remark Generator（备注生成器）
    ↓
CRM Adapter（CRM适配器）
    ↓
企业自研 CRM
```

**AI（人工智能）负责理解，业务规则负责决定，智能表格负责销售审核，Operations Console（运维/调试控制台）负责内部观测与排障，CRM（客户关系管理系统）负责最终归档；所有核心服务默认运行在 Docker（容器）中。**
